name: Build Skyrim NG

on:
  push:
    tags:
      - "*"
  workflow_dispatch:

env:
  VCPKG_ROOT: C:\vcpkg
  PRESET: clang-windows-vcpkg
  PROJECT_NAME: 00_VLRProxy

permissions:
  actions: read
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      - name: Update VCPKG
        run: |
          cd ${{ env.VCPKG_INSTALLATION_ROOT }}
          bootstrap-vcpkg.bat -disableMetrics
          vcpkg integrate install
      - name: Configure CMake
        run: cmake --preset ${{env.PRESET}}
      - name: Build
        run: cmake --build --preset ${{env.PRESET}} --config Release
      - name: Archive
        run: 7z a -tzip ${{ env.PROJECT_NAME }}.zip ./build/release-clang-windows-vcpkg/00_VLRProxy.dll ./build/release-clang-windows-vcpkg/00_VLRProxy.pdb
      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v7
        with:
          name: Skyrim
          path: |
            ./build/release-clang-windows-vcpkg/00_VLRProxy.dll
            ./build/release-clang-windows-vcpkg/00_VLRProxy.pdb
      - name: Create & Upload Release
        id: upload-release
        uses: softprops/action-gh-release@v3
        with:
          files: ${{ env.PROJECT_NAME }}.zip
          token: ${{ secrets.GITHUB_TOKEN }}
          name: ${{ env.PROJECT_NAME }} ${{ github.ref_name }}
        if: github.ref_type == 'tag'
